<div class="wrapper">
    {{ encore_entry_script_tags('app') }}
    <nav id="sidebar">
        <div class="sidebar_header">
            <h1>EMS</h1>
        </div>
        <ul class="sidebar_components">
            {% if is_granted("IS_AUTHENTICATED_FULLY") and  is_granted("ROLE_EMPLOYER") %}
            <ul class="navbar-nav m-auto">
                <li>
                    <a href="{{ path('index') }}" class="sidebar_component--link"><i class="fas fa-home"></i> Strona
                        Główna</a>
                </li>
                <li class="dropdown">
                    <a href="#" class="sidebar_component--link"><i class="fas fa-calendar-week"></i> Wydarzenia<i
                                class="fas fa-arrow-down dropdown_arrow"></i></a>
                    <div class="dropdown_content">
                        <a class="dropdown_content--link" href="{{ path('events') }}">Lista Wydarzeń</a>
                        <a class="dropdown_content--link" href="{{ path('events_add') }}">Dodaj Wydarzenie</a>
                    </div>
                </li>
                <li class="dropdown">
                    <a href="#" class="sidebar_component--link"><i class="fas fa-user-friends"></i> Pracownicy<i
                                class="fas fa-arrow-down dropdown_arrow"></i></a>
                    <div class="dropdown_content">
                        <a class="dropdown_content--link" href="{{ path('employees') }}">Lista Pracowników</a>
                        <a class="dropdown_content--link" href="{{ path('employees_add') }}">Dodaj Pracownika</a>
                    </div>
                </li>
                <li class="dropdown">
                    <a href="#" class="sidebar_component--link"><i class="fas fa-tasks"></i> Zadania<i
                                class="fas fa-arrow-down dropdown_arrow"></i></a>
                    <div class="dropdown_content">
                        <a class="dropdown_content--link" href="{{ path('tasks') }}">Lista Zadań</a>
                        <a class="dropdown_content--link" href="{{ path('tasks_add') }}">Dodaj Zadanie</a>
                    </div>
                </li>
                <li class="dropdown">
                    <a href="#" class="sidebar_component--link"><i class="fas fa-mountain"></i> Kamienie Milowe<i
                                class="fas fa-arrow-down dropdown_arrow"></i></a>
                    <div class="dropdown_content">
                        <a class="dropdown_content--link" href="{{ path('milestones') }}">Lista Kamieni milowych</a>
                        <a class="dropdown_content--link" href="{{ path('milestone_add') }}">Dodaj Kamień milowy</a>
                    </div>
                </li>
                <l1>
                    <hr>
                </l1>
                <li class="nav-item">
                    <a class="btn-log_out sidebar_component--link" href="{{ path('app_logout') }}"><i
                                class="fas fa-sign-out-alt"></i>
                        Wyloguj</a>
                </li>
                {% endif %}
                {% if is_granted("IS_ANONYMOUS") %}
                    <ul class="navbar-nav m-auto">
                        <li class="nav-item">
                            <a class="sidebar_component--link btn-log_in" href="{{ path('app_login') }}"><i
                                        class="fas fa-sign-in-alt"></i>
                                Zaloguj</a>
                        </li>
                    </ul>
                {% endif %}
                {% if is_granted("IS_AUTHENTICATED_FULLY") and  is_granted("ROLE_EMPLOYEE") %}
                <ul class="navbar-nav m-auto">
                    <li>
                        <a href="{{ path('index') }}" class="sidebar_component--link"><i class="fas fa-home"></i> Strona
                            Główna</a>
                    </li>
                    <li>
                        <a href="{{ path('events') }}" class="sidebar_component--link"><i
                                    class="fas fa-calendar-week"></i> Wydarzenia</a>
                    </li>
                    <li>
                        <a href="{{ path('tasks') }}" class="sidebar_component--link"><i class="fas fa-tasks"></i>
                            Zadania</a>
                    </li>
                    <li>
                        <a href="{{ path('milestones') }}" class="sidebar_component--link"><i
                                    class="fas fa-mountain"></i> Kamienie Milowe</a>
                    </li>
                    <l1>
                        <hr>
                    </l1>
                    <li class="nav-item">
                        <a class="btn-log_out sidebar_component--link" href="{{ path('app_logout') }}"><i
                                    class="fas fa-sign-out-alt"></i>
                            Wyloguj</a>
                    </li>
                    {% endif %}
                </ul>
            </ul>
        </ul>
    </nav>
</div>