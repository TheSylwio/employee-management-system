{% extends 'base.html.twig' %}

{% block title %}Hello IndexController!{% endblock %}

{% block body %}
    {{ include('layouts/navbar.html.twig') }}
    <div class="row">
        <div class="col text-center">
            <h1>Strona główna</h1>
            <p>🔥 TODO 🔥</p>
            <p><a href="{{ path('events') }}" class="btn btn-dark" role="button" aria-pressed="true">Wydarzenia</a></p>
        </div>
    </div>
{% endblock %}
